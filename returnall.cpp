#include <bits/stdc++.h>
using namespace std;

int code(string str, string out)
{
    if (str.empty())
    {
        cout << out << endl;
        return;
    }
}

int main()
{
    string str;
    cin >> str;

    string out = "";
    code(str, out);
    return 0;
}